{
  "name": "swap-workspace",
  "version": "0.1.0",
  "private": true,
  "description": "Swap workspace",
  "scripts": {
    "build": "cd www && npm run build",
    "dev": "cd www && npm run dev"
  },
  "dependencies": {
    "wasm-opt": "^1.3.0"
  }
}
